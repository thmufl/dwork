type MarketInfo = 
 record {
   description: text;
   id: principal;
   name: text;
 };
service : {
  createMarket: (text, text) -> (MarketInfo);
  deleteMarket: (text) -> (opt MarketInfo);
  marketInfos: () -> (vec MarketInfo);
}
